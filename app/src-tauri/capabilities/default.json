{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum the shell needs. Nothing is granted speculatively: this application talks to hardware, and every permission added here is reachable from a web context.",
  "windows": ["main"],
  "permissions": ["core:default", "log:default"]
}
