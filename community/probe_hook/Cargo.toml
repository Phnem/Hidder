[package]
name = "probe_hook"
version = "0.3.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "probe_hook"

[workspace]

[dependencies]
minhook = "0.7"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
winres = "0.1"
